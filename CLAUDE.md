# 프로젝트 지침

## HWPX 문서 생성 시 스킬 선택 (필수)

`.hwpx` 파일(공문·보고서·계획서·활동지 등 한글 문서)을 **생성/변환/편집**하는 요청을
받으면, 작업을 시작하기 전에 **항상 아래 두 스킬 중 어느 것을 사용할지 사용자에게 물어
선택하게 한다.** 임의로 하나를 골라 바로 진행하지 않는다.

| 선택지 | 스킬 이름 | 출처 | 특징 |
| --- | --- | --- | --- |
| A | `hwpx-jkf` | [jkf87/hwpx-skill](https://github.com/jkf87/hwpx-skill) | 명세(JSON/Markdown) 기반 생성·품질 검증, 다양한 템플릿(공문·보고서·계획서·활동지·요약·검토보고), 기존 양식 채우기 |
| B | `hwpx-kist` | [kist-aix/hwpx](https://github.com/kist-aix/hwpx) | XML-first 워크플로우, 공공기관 양식 보고서 자동 생성, 5종 표(일반·현황·예산·일정·체크리스트) |

### 규칙
1. hwpx 생성/편집 요청을 감지하면 위 두 선택지를 제시하고 사용자의 선택을 기다린다.
2. 사용자가 선택하면 해당 스킬(`hwpx-jkf` 또는 `hwpx-kist`)만 사용해 작업한다.
3. 사용자가 특정 스킬을 이미 명시했거나("kist로 만들어줘" 등) 이전 대화에서 선택을
   확정한 경우에는 다시 묻지 않고 그 스킬을 사용한다.
4. 두 스킬은 서로 독립적으로 설치되어 있으며 디렉터리는 각각
   `.claude/skills/hwpx-jkf`, `.claude/skills/hwpx-kist` 이다.
